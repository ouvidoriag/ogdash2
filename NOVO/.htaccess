# Configuração Apache para cPanel
# Redireciona todas as requisições para o servidor Node.js
# NOTA: O cPanel Application Manager gerencia o proxy reverso automaticamente
# Este arquivo é um fallback caso seja necessário

# Habilitar rewrite engine
RewriteEngine On

# Se o arquivo ou diretório existe, servir diretamente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirecionar para o servidor Node.js
# NOTA: A porta é gerenciada pelo cPanel Application Manager
# Se necessário, ajuste a porta abaixo (padrão: 3000)
RewriteRule ^(.*)$ http://localhost:3000/$1 [P,L]

# Configurações de segurança
<IfModule mod_headers.c>
    # Prevenir clickjacking
    Header always set X-Frame-Options "SAMEORIGIN"
    
    # Prevenir MIME type sniffing
    Header always set X-Content-Type-Options "nosniff"
    
    # Habilitar XSS protection
    Header always set X-XSS-Protection "1; mode=block"
</IfModule>

# Compressão Gzip (se mod_deflate estiver disponível)
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>

# Cache de arquivos estáticos
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType application/json "access plus 0 seconds"
</IfModule>

